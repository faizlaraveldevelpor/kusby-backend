# Copy to .env and fill values. Required for push token save (expo_push_token in profiles).
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_from_supabase_dashboard

# Optional (Stripe, etc.)
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
